services:
  - type: web
    name: universal-car-deal-scraper-no-cache
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true